{{ form_start(edit_form) }}
<div class="form-group">
    <label for="appbundle_product_name" class="required">Name</label>
    {{ form_widget(edit_form.name, {'attr': {'v-model': 'name ','class': 'form-control'}}) }}
</div>
<div class="form-group">
    <label for="appbundle_product_category" class="required">Category</label>
    {{ form_widget(edit_form.category, {'attr': {'v-model': 'category', 'class': 'form-control'}}) }}
</div>
<div class="form-group">
    <label for="appbundle_product_manufacturer" class="required">Manufacturer</label>
    {{ form_widget(edit_form.manufacturer, {'attr': {'v-model': 'manufacturer', 'class': 'form-control'}}) }}
</div>
<input type="submit" @click="onSubmitCreate" value="Create"/>
<input type="hidden" name="_csrf_token"
       value="{{ csrf_token('authenticate') }}"
>
{{ form_errors(edit_form) }}
{{ form_end(edit_form) }}
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}